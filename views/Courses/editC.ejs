<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Edit Course</h1>
    <hr>

    <form method="POST" action="/courses/edit/<%= course._id %>" enctype="multipart/form-data">
        <label for="title">Title:</label>
        <input type="text" name="title" value="<%= course.title %>" required>

        <label for="image">Image:</label>
        <input type="file" name="image" value="<%= course.image %>" required>
        <label for="image">Current Image:</label>
        <% if (course.image && course.image.data) { %>
            <img src="data:<%= course.image.contentType %>;base64,<%= course.image.data.toString('base64') %>" alt="Current Image"
            style="width: 200px; height: auto";>
        <% } %>

        <label for="link">Link:</label>
        <input type="text" name="link" value="<%= course.link %>" required>

        <label for="description">Description:</label>
        <textarea name="description"><%= course.description %></textarea>

        <label for="category">Category:</label>
        <select name="category">
            <option value="Women" <%= course.category === 'Women' ? 'selected' : '' %>>Women</option>
            <option value="Children" <%= course.category === 'Children' ? 'selected' : '' %>>Children</option>
        </select>

        <label for="language">Language:</label>
        <select name="language">
            <option value="English" <%= course.language === 'English' ? 'selected' : '' %>>English</option>
            <option value="Urdu" <%= course.language === 'Urdu' ? 'selected' : '' %>>Urdu</option>
        </select>

        <label for="date">Date:</label>
        <input type="date" name="date" value="<%= course.date.toISOString().split('T')[0] %>" required>

        <button type="submit">Update Course</button>
    </form>

</body>
</html>
